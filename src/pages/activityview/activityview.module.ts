import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ActivityviewPage } from './activityview';

@NgModule({
  declarations: [
    ActivityviewPage,
  ],
  imports: [
    IonicPageModule.forChild(ActivityviewPage),
  ],
})
export class ActivityviewPageModule {}
